---
description: 面经知识库中 Markdown 图片与静态资源的引用规范
globs: docs/**/*.md
alwaysApply: false
---

# 静态资源与图片引用规范

在维护面经知识库的 Markdown 文档时，请遵循以下静态资源管理规范：

## 1. 存放位置
- **所有图片必须统一存放在 `docs/public/images/` 目录下**。
- 请勿将图片散落在各个子目录中，也请勿使用相对路径（如 `../assets/`）存放图片。
- 命名规范：图片命名应尽量采用全小写英文、数字和中划线（如 `ab-test-funnel.png`），避免使用包含空格或中文字符的文件名。

## 2. Markdown 中的引用路径
- 引用图片时，**必须使用绝对路径 `/images/xxx.png`**。
- **严禁使用相对路径**（如 `./images/xxx.png` 或 `../../public/images/xxx.png`），因为 VitePress 会自动将 `docs/public/` 下的文件映射到站点根目录。

```markdown
// ❌ 错误示范：使用相对路径或带 public 的路径
![漏斗图](../../public/images/funnel.png)
![漏斗图](./images/funnel.png)

// ✅ 正确示范：直接以 /images/ 开头
![漏斗图](/images/funnel.png)
```

## 3. 注意事项
- 由于本地拆分脚本 `docs:split` 的存在，如果在 `归档/` 目录中的原始笔记里插入了图片，请确保在同步更新内容后，一并检查图片引用路径是否符合最终 VitePress 的 `/images/...` 规范，并且图片文件已经手动放置在 `docs/public/images/` 中。
