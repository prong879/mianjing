---
description: 知识库的统一内容排版、格式与静态资源管理规范
globs: docs/**/*.md
alwaysApply: false
---

# 知识库内容规范 (Content Standards)

在编写、扩充面经与知识库时，请严格遵守此高内聚规范。所有的 Markdown 文件均直接在 `docs/` 目录下编辑。

**🔴 原始文件与归档文件保护红线：**
- **绝不删除原始文件**：整合资料后，绝对禁止 AI 擅自删除用户提供的任何源文件。
- **归档文件绝对只读**：对于 `归档/` 目录下的内容，**严禁删除，也严禁修改（归档了就是不改了）**。只能从中读取信息并整理到 `docs/` 目录中。

## 1. 静态资源（图片等）管理
- **存放位置**：所有新增图片必须保存到 `docs/public/images/` 下。
- **命名规范**：全小写英文、数字和中划线（如 `ab-test-funnel.png`），避免空格和中文。
- **引用路径**：Markdown 中引用必须使用绝对路径 `/images/xxx.png`。
  - ✅ 正确：`![示例图](/images/demo.png)`
  - ❌ 错误：`![示例图](../../public/images/demo.png)`、`![示例图](./images/demo.png)`

## 2. Frontmatter（元数据）与标签 (Tags)
对于所有在 `docs/` 下新建的文件，必须在顶部添加 YAML Frontmatter 并在右侧渲染大纲。如果内容涉及特定公司（如字节跳动、腾讯）或特定技术标签，请使用 `tags` 字段标注，以利用 VitePress 的本地搜索功能：
```markdown
---
title: "你的页面标题"
outline: deep
tags:
  - 字节跳动
  - 腾讯
  - 商业分析
---
```

对于追加到已存在文件中的面试题，你可以在该题目的标题或引用块中加上相关标签信息。如果该题涉及笔试、机试或需要手撕代码，务必在标签中标注。例如：
`> 🏷️ 标签：字节跳动，2025秋招，一面，笔试，手撕代码`。

## 3. 严格的目录结构与扩充原则 (Strict Directory Structure)
本知识库采用**极度收敛的固定结构**，严禁 AI 随意创建新的顶级目录或擅自改变分类逻辑。

- **知识库 (`docs/knowledge/`)** 采用“6大能力支柱”：
  1. `01-data-infrastructure/` (数据与工程基础: SQL, 数仓)
  2. `02-python-data/` (Python 数据处理)
  3. `03-statistics-experiment/` (统计与实验: AB测试)
  4. `04-business-product/` (业务与数据产品: 商分, BI)
  5. `05-ai-ml/` (机器学习与 AI Agent)
  6. `06-quant-finance/` (量化研究)
- **面试题库 (`docs/interviews/`)** 采用“4大岗位方向”：
  1. `01-data-analytics/` (数据与分析岗)
  2. `02-ai-agent/` (AI Agent 岗)
  3. `03-quant-finance/` (量化研究岗)
  4. `04-product-manager/` (产品经理岗)

**🔴 扩充红线规则：**
1. 任何新引入的知识或面试题，**必须优先归入现有的二级子目录和已有的 Markdown 文件中**。
2. 只有当新内容**完全无法**融入现有结构时，才允许新建 Markdown 文件。
3. **绝对禁止** AI 擅自创建新的能力支柱（如 `07-xxx`）或新的岗位方向（如 `05-xxx`）。如果遇到超出现有框架的新领域（例如突然要加入“前端开发”面经），AI **必须停止归类**，并强制向用户提问：“新内容超出现有结构，是否需要为你创建新的顶级目录？” 获得用户明确同意后方可行动。

## 4. 文本排版与面试题格式
- **题库纯粹性红线**：面试题库 (`docs/interviews/`) 必须保持纯粹，只包含“题目 + 标签（如果有） + 解答”。任何大段的知识点讲解、方法论科普，必须合理地抽取并移动到知识库 (`docs/knowledge/`) 的对应专题中，严禁在题库中大篇幅夹杂知识科普。
- **标题**：优先使用 `##` 和 `###`，标题与正文之间空一行。
- **标点排版**：中英文之间、中文与数字之间必须保留一个半角空格（如 `在 Python 中处理 100 万条数据`）。
- **专有名词**：严格区分大小写（如 `MySQL`, `Python`, `AI Agent`）。
- **代码块**：包含代码的题目或答案，必须严格使用 Markdown 代码块（如 ` ```sql ` 或 ` ```python `），并确保缩进正确、注释清晰。不要将大段代码混在普通文本中。
- **面试题与解答规范**：面试题及其参考答案必须严格遵循以下 Markdown 结构，保证知识库的视觉一致性和易读性：
  1. 题目本身必须作为无序列表项，且内容加粗呈现，如：`- **什么是 RAG？它的优缺点是什么？**`
  2. 如果题目有关联的标签（如公司名、考察点），应该在题目下方加上空行，然后使用块引用形式加上标签，如：`> 🏷️ 标签：字节跳动，笔试`
  3. 标签（或题目）和解答内容之间必须保留一个空行。
  4. 解答内容必须以块引用（`>`）包裹，并统一使用 `**参考回答**：`（或 `**参考答法**：`）开头。注意冒号必须在加粗标识的外部，即 `**参考回答**：` 而非 `**参考回答：**`。

**标准面试题模板示例：**

```markdown
- **什么是 RAG？它的优缺点是什么？**

> 🏷️ 标签：大模型，字节跳动

> **参考回答**：RAG（Retrieval-Augmented Generation）是检索增强生成技术。
> 优点：能够缓解大模型的幻觉问题，提供私有知识。
> 缺点：依赖检索的准确性，增加了一定的延迟和系统复杂度。

- **XXXXXXXX（题目）**

> 🏷️ 标签：XX

> **参考回答**：XXXXXXXX
```
