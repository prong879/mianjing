---
description: 面经知识库的 Markdown 编写规范与 Frontmatter 格式
globs: docs/**/*.md, 归档/**/*.md
alwaysApply: false
---

# Markdown 编写与格式规范

在编写或扩充知识库内容时，请严格遵守以下排版与格式规范，以保证 VitePress 站点的渲染效果和阅读体验。

## 1. Frontmatter（元数据）
对于所有直接在 `docs/` 目录下手工新建的 `.md` 文件，必须在文件顶部添加 YAML Frontmatter。
包含 `title` 和 `outline: deep` 属性，用于在右侧生成深层大纲导航：

```markdown
---
title: "你的页面标题"
outline: deep
---
```
*(注：通过 `docs:split` 自动生成的文件已由脚本自动注入，无需手动添加)*

## 2. 标题规范
- 优先使用二级标题 `##` 和三级标题 `###`，不要过度嵌套。
- 标题与正文之间必须保留一个空行。
- 标题的 `#` 号后必须跟一个空格，如 `## 面试高频题`。

## 3. 文本与标点符号
- 中英文混排时，**中文和英文、中文和数字之间必须保留一个半角空格**（例如：`在 Python 中使用 pandas 处理数据`）。
- 专有名词请注意大小写（如 `MySQL`, `Python`, `JavaScript`, `AI Agent`），禁止拼写为 `mysql`, `python` 等。

## 4. 代码块
- 所有代码块必须指定语言高亮标签（如 `python`, `sql`, `javascript`, `bash`）。
- 不要随意在代码块中省略必要的缩进。

```sql
SELECT 
    user_id,
    COUNT(order_id) AS order_cnt
FROM orders
GROUP BY user_id;
```
