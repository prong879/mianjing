---
description: VitePress 侧边栏及导航栏更新规范
globs: docs/.vitepress/config.ts, docs/**/*.md
alwaysApply: false
---

# VitePress 配置更新规范

当你在 `docs/` 目录下**手工创建**了全新的 Markdown 页面（非脚本自动生成）后，必须更新 VitePress 的配置文件以暴露该页面给用户。

## 1. 修改 `config.ts`
VitePress 的配置文件位于 `docs/.vitepress/config.ts`。
新增页面后，必须在 `themeConfig.sidebar` 中找到对应的模块，并添加路由项：

```typescript
// 示例：在指南模块中新增页面
'/guide/': [
  {
    text: '指南',
    items: [
      { text: '如何使用本站', link: '/guide/how-to-use' },
      // 新增你的页面
      { text: '你的新页面标题', link: '/guide/your-new-page' },
    ],
  },
],
```

## 2. 注意路由路径
- `link` 属性的值必须以 `/` 开头，并且**不需要带 `.md` 后缀**。
- 确保路由路径与实际文件的相对路径严格一致。

## 3. 脚本生成的页面无需手动配置
注意：如果新增的内容是通过 `npm run docs:split` 从 `归档/` 目录生成的，并且已经在 `split-interview-notes.mjs` 中配置了映射表，则不需要在 `config.ts` 中手动乱加，除非你想改变站点的侧边栏结构。
