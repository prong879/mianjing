---
description: VitePress 侧边栏（Sidebar）及导航更新规范
globs: docs/.vitepress/config.ts, docs/**/*.md
alwaysApply: false
---

# VitePress 配置更新规范 (Sidebar Config)

现在所有的页面新增都直接在 `docs/` 目录下进行。当添加了全新的 Markdown 页面后，**必须同步更新 `docs/.vitepress/config.ts`**，否则页面无法在侧边栏显示。

## 1. 侧边栏配置修改
在 `themeConfig.sidebar` 找到对应的板块（如 `/knowledge/` 或 `/interviews/`），在 `items` 数组中追加路由：

```typescript
'/knowledge/': [
  {
    text: '你的新模块',
    items: [
      { text: '页面标题', link: '/knowledge/your-folder/new-page' },
    ],
  },
],
```

## 2. 路由路径注意事项
- `link` 的值必须以 `/` 开头。
- **不需要**包含 `.md` 后缀。
- 确保层级结构反映出它是属于 `知识体系（knowledge）` 还是 `面试题（interviews）`，高内聚低耦合。
