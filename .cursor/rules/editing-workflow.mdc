---
description: 知识库内容编辑与生成工作流规范
globs: docs/**/*.md, 归档/**/*.md
alwaysApply: false
---

# 知识库内容编辑工作流规范

本项目采用了“从源 Markdown 自动生成最终 VitePress 页面”的架构。在修改面经或知识库内容时，必须遵循以下工作流，否则修改将被覆盖丢失！

## 1. 识别自动生成的文件
如果被要求修改 `docs/knowledge/` 或 `docs/interviews/` 下的 Markdown 文件，**必须先检查文件内容**。
如果文件顶部包含如下引用提示：
`> 原文备份路径：...（与站点并列，编辑时请直接改归档目录下源文件后重新运行 npm run docs:split）`
**绝对禁止直接编辑该 `docs/` 下的文件！**

## 2. 正确的编辑流程
如果遇到上述自动生成的文件，请执行以下步骤：
1. 读取该提示中指定的 `归档/` 目录下的源 Markdown 文件。
2. 在源文件（`归档/` 下的文件）中找到对应的段落并进行修改/添加。
3. 修改完成后，在终端运行拆分脚本：
   ```bash
   npm run docs:split
   ```
4. 运行完毕后，检查 `docs/` 下的对应文件是否已成功更新。

## 3. 手工维护的文件
对于 `docs/guide/` 下的文件或没有“原文备份路径”提示的文件，可以直接在原文件上进行编辑。
这些文件不会被脚本覆盖。
